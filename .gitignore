node_modules
.env*
# Las plantillas SÍ se versionan: provision.sh las necesita en el checkout.
!.env.example
!.env.production.example
.eve
.vercel
.next
.output
.nitro
dist
.DS_Store
*.tsbuildinfo

.env

# Build anterior que guarda deploy.sh para poder hacer rollback.
.output.anterior
.output.fallido

# Capturas, descargas y borradores del modo de ejecucion local.
.sri-datos

# Cache de npm del usuario de servicio (su HOME es el directorio de la app).
.npm

# Build de la UI.
web/dist
